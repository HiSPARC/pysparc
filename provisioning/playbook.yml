---
- hosts: vagrant
  tasks:
    - include: vagrant.yml

- hosts: raspberry_pi
  tasks:
    - include: raspberry_pi.yml
  handlers:
    - include: handlers.yml

- hosts: jessie
  tasks:
    - include: apt-prerequisites.yml
    - include: jessie.yml
    - include: apt-upgrade.yml
  handlers:
    - include: handlers.yml
  tags: jessie

- hosts: all
  tasks:
    - include: common.yml
    - include: prerequisites.yml
    - include: pysparc.yml
  handlers:
    - include: handlers.yml
